<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zblog.stats.infrastructure.mybatis.VisitMapper">

  <insert id="insertVisit" parameterType="com.zblog.stats.domain.VisitEventInput">
    insert into visit_events (
      visitor_id, event_type, url, hostname, title, referrer, language, screen,
      article_id, event_name, event_data, duration_seconds, ip, user_agent, created_at
    ) values (
      #{visitorId}, #{eventType}, #{url}, #{hostname}, #{title}, #{referrer}, #{language}, #{screen},
      #{articleId}, #{eventName}, #{eventData}, #{durationSeconds}, #{ip}, #{userAgent}, #{createdAt}
    )
  </insert>

  <update id="incrementPublishedArticleViewCount">
    update articles
    set view_count = view_count + #{delta},
        updated_at = updated_at
    where id = #{articleId} and status = 'PUBLISHED'
  </update>

  <select id="articleViewCount" resultType="long">
    select view_count from articles where id = #{articleId}
  </select>
</mapper>
