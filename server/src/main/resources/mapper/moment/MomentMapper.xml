<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zblog.moment.infrastructure.mybatis.MomentMapper">
  <select id="findAll" resultType="map">
    select * from moments
  </select>

  <select id="rowsById" resultType="map">
    select * from moments where id = #{id}
  </select>

  <insert id="insertMoment" parameterType="map" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
    insert into moments (content_json, is_publish, publish_time)
    values (#{contentJson}, #{publish}, #{publishTime})
  </insert>

  <update id="updateMoment">
    update moments
    set content_json = #{contentJson}, is_publish = #{publish}, publish_time = #{publishTime}, updated_at = current_timestamp
    where id = #{id}
  </update>

  <delete id="deleteMoment">
    delete from moments where id = #{id}
  </delete>
</mapper>
